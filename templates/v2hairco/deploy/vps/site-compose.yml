# /srv/sites/<name>/docker-compose.yml — a fully self-contained static site.
# Copy this folder per site; change only the two label lines + router name.
#   cd /srv/sites/<name> && docker compose up -d
services:
  web:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./public:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    labels:
      - traefik.enable=true
      - traefik.http.routers.v2hairco.rule=Host(`v2hairco.oluxstudio.com`)
      - traefik.http.routers.v2hairco.entrypoints=websecure
      - traefik.http.routers.v2hairco.tls.certresolver=le
    networks: [edge]

networks:
  edge:
    external: true
